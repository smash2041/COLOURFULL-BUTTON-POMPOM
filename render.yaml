services:
  - type: web
    name: host-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: TURSO_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
      - key: UPI_ID
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
